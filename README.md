Refer to man/Function Descriptions.Rmd for package and function descriptions! Download the .Rmd file or the .html file for optimal reading experience. 

To install the CheckNorm package, use this code:

```r
#install.packages("devtools")  # if not already installed
library("devtools") # as I'm sure most you have devtools installed!
devtools::install_github("lindgrenrebecca/CheckNorm")
library(CheckNorm)
```
